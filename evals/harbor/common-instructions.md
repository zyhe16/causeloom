# Common benchmark instructions

Work autonomously on the task in the provided container. Treat the task instruction as the product requirement and make the implementation correct, robust, and maintainable.

The benchmark sandbox blocks general outbound network access. Do not attempt to search the web, query remote documentation, fetch source code, install packages from a registry, or obtain outside solution help. Use only the files and tools already present in the task environment.

Do not look for, access, or infer hidden grader tests or the reference solution. They are intentionally unavailable during the agent phase. Do not weaken checks, bypass the production path, or hard-code visible examples. Run relevant local verification when it is available, and leave the completed implementation in the task workspace.

The experimental condition, when present below, is the sole additional coding-policy condition for this run.
