# Naming

- **Display name:** Causeloom
- **Skill ID:** `causeloom`
- **Repository:** `zyhe16/causeloom`
- **Invocation:** `$causeloom`

The name combines **cause** and **loom**. The skill traces the causal structure
of a software problem, then weaves requirements, repository evidence,
implementation, and verification into a finished result.

Use **Causeloom** in prose and `causeloom` for directories, packages, commands,
condition identifiers, and repository paths.
